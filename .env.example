# Copy to `.env` and adjust. Loaded automatically by `jupyter_base.config.load_settings`.

# Human-readable name surfaced in examples and logs
JUPYTER_BASE_APP_NAME=jupyter-base

# development | staging | production
JUPYTER_BASE_ENV=development

# Set to true/1/yes to mark debug mode in examples
JUPYTER_BASE_DEBUG=false

# Writable data directory (created by your workflows as needed)
JUPYTER_BASE_DATA_DIR=./data

# OpenAI (optional): this package reads these without copying .env into os.environ.
# Prefer a key file for notebooks so the kernel can run with OPENAI_API_KEY unset:
#   mkdir -p .secrets && printf '%s\n' 'sk-...' > .secrets/openai_api_key
#   JUPYTER_BASE_OPENAI_KEY_FILE=.secrets/openai_api_key
# Then start Lab with: make run-jupyter JUPYTER_STRIP_OPENAI_ENV=1
# OPENAI_API_KEY=
# JUPYTER_BASE_OPENAI_KEY_FILE=.secrets/openai_api_key

# Default model for OpenAIClient.chat_completion / complete_text when model= is omitted
JUPYTER_BASE_OPENAI_MODEL=gpt-4o-mini

# Docker Compose only: JupyterLab token (empty = no token; fine on localhost only)
# JUPYTER_LAB_TOKEN=

# Optional: host port when using docker compose (defaults to 8888)
# JUPYTER_PORT=8888
